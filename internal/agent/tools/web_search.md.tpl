Search the web; returns titles, URLs, and snippets. The backend (Firecrawl, Exa, or DuckDuckGo) is selected via the Search & Fetch Backend command. Follow up with web_fetch to get full page content.
{{- if .GhAvailable }} For GitHub searches when an exact repo name, issue, or link is provided, use `gh search` in bash instead.{{- end }}
